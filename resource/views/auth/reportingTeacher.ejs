<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../css/style-register.css" />
    <link rel="stylesheet" href="../../css/style-reporting.css" />
    <link rel="shortcut icon" href="../img/favicon.png" type="image/x-icon" />
    <title>Plans Classes Admin</title>
  </head>
  <body>
    <section>
      <header>
        <div class="header">
          <form action="/logout" method="post">
            <button
              href="/logout"
              class="btn_header"
              style="padding: 9px !important"
            >
              خروج
            </button>
          </form>
          <a type="submit" href="/auth/reporting" class="btn_header">
            گزارش گیری
          </a>
          <a type="submit" href="/teacher/planDays" class="btn_header">
            تعریف برنامه
          </a>
          <a
            type="submit"
            href="<%= auth.user.admin ? '/admin' : '/teacher' %>"
            class="btn_header"
          >
            صفحه اصلی
          </a>
        </div>
      </header>
    </section>

    <section class="body">
      <form class="form" method="POST" novalidate>
        <main class="main_form_register">
          <h1 class="head">کلاس های استاد مورد نظر</h1>
          <main class="back_form" id="back_form">
            <table>
              <tr id="headerTable">
                <th>روز های هفته</th>
                <th>کد کلاس</th>
                <th>رشته</th>
                <th>نام استاد</th>
                <th>نام درس</th>
                <th>نوع</th>
                <th>ساعت شروع</th>
                <th>ساعت پایان</th>
              </tr>

              <% for(var i = 0 ; i < repe[0].length ; i++) { %>
              <tr>
                <td><%= repe[1][i] %></td>
                <td><%= repe[2][i] %></td>
                <td><%= repe[3][i] %></td>
                <td><%= repe[4][i] %></td>
                <td><%= repe[5][i] %></td>
                <td><%= repe[6][i] %></td>
                <td><%= repe[7][i] %></td>
                <td><%= repe[8][i] %></td>
              </tr>
              <% } %>
            </table>
          </main>
        </main>
      </form>
    </section>
  </body>
</html>
